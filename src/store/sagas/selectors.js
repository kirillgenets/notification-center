export const getCurrentPage = (state) => state.currentPage;

export const getCompletionStatusFilter = (state) => state.completionStatusFilter;

export const getCategoryFilter = (state) => state.categoryFilter;

export const getTeam = (state) => state.team;
